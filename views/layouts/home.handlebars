<h1>Lista de Posts!</h1>

{{#each posts}}

    <h1>{{dataValues.titulo}}</h1>
    <p>{{dataValues.conteudo}}</p>
    <small>{{dataValues.createdAt}}</small><br>
    <a href="/del/{{dataValues.id}}"><button>Deletar</button></a> 
    <hr>
    

{{/each}}